# TikTok native extension

Effective 2026-09-14, every generated-content variant that is actually published through the authorized Android phone also receives a TikTok Android-native test when TikTok supports the format. TikTok is additional to—not a substitute for—the full Facebook, Instagram and Threads coverage.

Scope:

- Route: Android native only. No TikTok API work is authorized by this extension.
- Account: only `@sabiduriabolsillo` after identity remains visible in the composer.
- Assets: the same factual, generated lab master may be adapted to TikTok's verified native format. Never reuse an internal lab announcement or publish an unreviewed draft.
- Formats: determine photo-post, video-post and any interactive option from the actual signed-in native composer. Each unsupported or blocked combination gets a coverage cell with evidence; it is never assumed.
- Quality: follow `visual-qa-gate.md` before every public submission, including a clean TikTok-native preview with actual sound/labels/effects. Select destination-native music for every still image and for any video without an embedded soundtrack; the final QA preview must show that label without obscuring content.
- Measurement: record TikTok's own view, reach, watch-time/retention and engagement definitions separately at 24 h, 72 h and 7 d. Do not merge them with Meta metrics.

Current state: the Android package and the signed-in brand profile are verified; TikTok's native puzzle verification blocks composer access. After the user completes it, run a non-public import/draft/preview probe first, then add the applicable TikTok cells to `coverage.json` and space them from the corresponding Meta release.
