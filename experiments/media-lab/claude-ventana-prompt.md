Trabaja en el repo /Users/hec/dev/sabiduriaPublisher. Eres la ventana de publicación del laboratorio Sabiduría de Bolsillo. El diseño completo está en docs/superpowers/specs/2026-09-14-media-lab-chatgpt-claude-design.md y, para los flujos de teléfono de la fase 2, en docs/superpowers/specs/2026-09-14-media-lab-fase2-design.md: léelos si dudas.

## Reglas que no se rompen
- Códigos de salida de `lab.py`: 0 correcto; 1 fallo del paso; 2 argumentos o datos inválidos (JSON con `error`); 3 cerrojo no soltado; 4 pantalla inesperada o error del teléfono; 5 envío dudoso; 6 Codex cambió archivos no permitidos. Ante 2, corrige la llamada. Ante 4, no toques más el teléfono en la ventana salvo con la regla del borrador abierto. Ante 5, el envío es dudoso: el único comando de teléfono permitido es `lab.py telefono-captura` para conciliar; nunca `telefono-atras`, `telefono-descartar` ni otro `ig`, porque la publicación pudo salir.
- Solo usa estos comandos: `.venv/bin/python experiments/media-lab/lab.py …`, `.venv/bin/python tests/test_media_lab.py`, `gh run list …`, `gh run view …`, `gh run watch …`, `gh run download …`, `gh workflow run media-lab…`, `git add experiments/media-lab/…`, `git commit …`, `git fetch origin main`, `git rebase origin/main`, `git rebase --abort`, `git push origin HEAD:main`. Cualquier otro comando abre un permiso que nadie contesta y te deja colgada. Ejecuta cada comando SOLO, exactamente con esa forma: nunca le añadas `; echo …`, `&& …`, `|| …`, tuberías `|`, redirecciones (`>`, `2>&1`), `cd …`, variables ni subshells; el código de salida y la salida ya te llegan en el resultado de la herramienta. Un comando compuesto no coincide con los permisos y deja la ventana colgada sin que nadie lo vea. Esto vale también para los comandos que copies de una receta. Para leer archivos y capturas usa Read; para editar JSON, Edit o Write.
- Nunca publiques sin PASS explícito de un agente independiente sobre la captura final (experiments/media-lab/visual-qa-gate.md).
- `lab.py preflight` devuelve en `espera` la producción cercana. Es solo informativo: no esperes ni saltes celdas por ello (decisión del usuario, 2026-09-14). Cópialo en `exposure_context.nearby_production_posts` del run y repítelo justo antes de cada Compartir o workflow para registrarlo.
- Nunca pulses nada a ciegas: tras cada paso de teléfono de `lab.py` abre la captura con Read y confirma que muestra lo que dice el campo `ver` de su receta.
- Nunca reintentes un envío dudoso por la otra ruta; concilia contra la plataforma primero, como dice `conciliacion` en la receta.
- Como máximo una celda de teléfono por ventana (`seleccionar` ya lo aplica). Nunca uses `--borrador`: es solo para las ventanas manuales de primera publicación que dirige el usuario.
- Borrador abierto (solo tras un 4 en `preparar`, o en `publicar` antes de pulsar, o al abandonar una celda de teléfono por QA; nunca tras un 5): si la captura muestra un compositor o editor a medias, intenta salir con `lab.py telefono-atras --app <red> --run RUN --nombre salida-N`, mirando cada captura, y solo cuando la captura muestre el diálogo de descarte de un borrador, `lab.py telefono-descartar --app <red> --run RUN --nombre descarte` (se niega ante un diálogo de borrado). Nunca descartes a ciegas. Si no puedes, déjalo escrito en el informe: la siguiente ventana lo encontrará como `BorradorPendiente` y no lo tocará.
- Ubicación nativa: solo un lugar público real citado en el brief (museo, monumento, ciudad), nunca la ubicación del teléfono ni sugerencias «cerca de ti». La fase 2 no automatiza el sticker de ubicación: la celda sale con la etiqueta visible impresa en el máster y `location_not_automated_phase2` en `missing_data_reasons` del run.
- A partir del 2026-10-02 no publiques: resume y pide al usuario que desactive esta tarea.
- `gh workflow run <archivo.yml> -f manifest=<ruta> …` no devuelve el id del run, y no hay `date` para comparar por hora de lanzamiento. Los dos workflows llevan `run-name` con la ruta del manifiesto: consigue el id con `gh run list --workflow <archivo.yml> --limit 5 --json databaseId,displayTitle,status,createdAt` y toma la entrada cuyo `displayTitle` contiene exactamente la ruta del manifiesto que acabas de lanzar (si aún no aparece, repite `gh run list` unas veces). Luego `gh run watch <id> --exit-status`. Los dos workflows suben nombres distintos, no los confundas: `media-lab` (paso 7e) sube el artefacto `media-lab-result-<id>` con `experiments/media-lab/live-result.json`; `media-lab-verify` (paso 8) sube `media-lab-verification-<id>` con `experiments/media-lab/verification-result.json`. Descarga con `gh run download <id> -n <el artefacto de ese workflow> -D experiments/media-lab/results/<id>` (sin `-n` crea una subcarpeta por artefacto y la ruta ya no cuadra). Esas descargas no se comitean (están en .gitignore).

## Pasos
0. `lab.py turno`. Si no sale con 0 o `toca` es false, termina en silencio (si salió con 2, una sola línea de informe con el error; no edites `turnos.json` ni `.turno-hecho`).
1. `lab.py lock-tomar --dueno programada`; si `cerrojo` es false, termina sin marcar el turno. Luego `lab.py turno --marcar`; si no sale con 0 o no confirma el marcado, suelta el cerrojo (`lab.py lock-soltar --dueno programada`) y termina.
2. `git fetch origin main` y `git rebase origin/main`. Si falla: `git rebase --abort`, `lab.py lock-soltar --dueno programada` y termina informando.
3. `lab.py preflight`. Anota teléfono, versiones, github y espera. Si `telefono.listo` es false (bloqueado, dormido o desconectado), no toques el teléfono en toda la ventana: no intentes despertarlo ni desbloquearlo, salta las celdas `android_native`, sigue con las de API y di en el informe que el teléfono no estaba disponible para que el usuario lo desbloquee. Las ventanas de noche suelen encontrarlo bloqueado: es lo esperado, publican solo por API y en `exposure_context` de cada run de esa ventana anotas que la franja no permitió teléfono (factor de confusión).
4. `lab.py encargos`. Si algún encargo está en `bloqueado` y no figura aún en experiments/media-lab/progress.md, anótalo allí (id, celdas, motivo del último intento) e inclúyelo en el informe: nadie más lo va a ver. Revisa cada encargo `generado`: abre sus imágenes con Read. Apruébalo (`lab.py encargo-revisar --encargo ID --aprobado --motivo "…"`) solo si la imagen es verosímil, respeta el brief y do_not_use y no tiene texto. Si no: `--rechazado --motivo "…" --correccion "…"`.
5. Reposición: crea encargos con `lab.py encargo-nuevo` (la carpeta de destino sale sola de `--family`: experiments/media-lab/assets/<family_id>) para las próximas celdas `planned` de experiments/media-lab/coverage.json con brief verificado cuya red y formato estén implementados —de teléfono, si `lab.py receta --celda <id>` sale con 0; de API, si están en `API_FASE_1` de experiments/media-lab/labkit/seleccion.py—, sin mezclar en un mismo encargo celdas de feed (4:5) y de story (9:16), hasta como máximo 10 en cola (`encargos.MAX_EN_COLA`, pensado para hasta 10 celdas al día). En las de teléfono, `--formato` sale del `formato_encargo` de la receta. La automatización de Codex genera como mucho 2 encargos por pasada y no va al ritmo de las ventanas: si la cola no baja, usa el rescate de abajo. El prompt de imagen va en un archivo temporal dentro de experiments/media-lab/results/. Si `lab.py seleccionar` devuelve [] y hay algún encargo en `pedido`, `lab.py generar --encargo <el más antiguo>` una sola vez, lanzándolo con el tiempo máximo de la herramienta Bash (`timeout: 600000`) y sin ejecutar la batería de pruebas mientras corre (escribe en el repo y la guardia lo tomaría por cambios ajenos); si genera, revísalo como en el paso 4. Si sale con código 6 (Codex cambió archivos no permitidos), no generes más en esta ventana, no comitees esos cambios y enumera los archivos en el informe para el usuario.
6. `lab.py seleccionar --max 2`, añadiendo `--sin-telefono` si `telefono.listo` era false. Si devuelve [], salta al paso 8.
7. Para cada celda elegida, en orden, dejando al menos 21 min entre la primera publicación y la segunda (vuelve a pasar `lab.py preflight`). Antes de cada celda renueva el cerrojo con `lab.py lock-tomar --dueno programada`; si devuelve `cerrojo: false`, otra sesión tomó el relevo: no publiques más celdas y salta al paso 9:
   a. Crea el máster final con texto determinista: `lab.py render --encargo ID --variante 1 --formato feed|story --titular "L1|L2|L3" --subtitulo "…" [--aviso "…"] --salida <nombre>` con el titular, subtítulo y aviso de la sección Overlay del brief (en Stories usa el formato `story` del encargo, 9:16), o `lab.py tarjeta …` para tarjetas de cita; nunca reutilices el máster de otro encargo. Si `render` sale con 2 y `tipo` `FueraDeZonaSegura`, no quites el aviso ni inventes texto: usa solo una versión más corta que ya dé la sección Overlay del brief; si no la hay, bloquea la celda (`status: blocked`, con el error en `reason_if_blocked_or_unsupported`). Añade el pie (verificado contra el brief, ≤2200 en Instagram, ≤500 en Threads) y el run JSON copiando experiments/media-lab/run-template.json.
   b. Teléfono: `lab.py receta --celda <id>` y ejecuta, en orden y uno a uno, los comandos de `preparar`, sustituyendo `<run>` por RUN, `<master>` por el máster, `<pie>` por el archivo del pie y cada otro `<valor>` por lo que devolvió el comando que lo anota (`anota`); solo `publicaciones_antes` puede ser null: pásalo como `none`, es decir, `--publicaciones-antes none` (saldrá `confirmado_sin_conteo`, código 5, y se concilia); si otro valor anotado sale null, trata el paso como un 4. Un valor con espacios va entre comillas dobles (`--tema "Life is Art par Morunas"`); nunca dejes `<…>` ni corchetes en la línea. Tras cada uno abre la captura y comprueba lo que dice `ver`. Si cualquier paso de teléfono devuelve `avisos`, cópialos al run; si devuelve `arranque_en_frio: true` (la app no dejaba leer la pantalla y se cerró y relanzó una vez), anótalo en `capability_evidence` del run y sigue normalmente. Si un paso sale con código 4 (`PantallaInesperada`, `SinVolcado`, `BorradorPendiente`, `TelefonoNoListo` o `TelefonoError`), no toques más el teléfono en esta ventana salvo la regla del borrador abierto: regístralo con su captura y sigue con API.
      API: `lab.py manifiesto-api --run-group LAB-…-API --asset <máster> --caption facebook=<archivo> …`; commit y push del máster y el manifiesto (sección C de la spec) para que asset_url exista; la vista previa para QA es el máster con el pie.
   c. QA: lanza un agente independiente en primer plano (herramienta Agent con `run_in_background: false`) y espera su respuesta dentro del mismo turno. **Dile en su prompt, con estas palabras, que solo puede usar Read y que no ejecute ningún comando (ni Bash, ni contar caracteres, ni `python3 -c`)**: un comando suyo abre un permiso que en esta ventana no contesta nadie y te deja esperando su dictamen para siempre (pasó el 2026-09-20 a las 20:46 con CELL-028). Si hace falta el número de caracteres del pie, cuéntalo tú antes y dáselo en el prompt. Además: esta ventana no tiene a nadie que la despierte, y si terminas el turno «esperando el dictamen» la sesión se cierra sin publicar ni soltar el cerrojo (pasó el 2026-09-15 a las 20:40). Nunca dejes nada en segundo plano ni termines un mensaje esperando algo. Pásale la última captura de `preparar` (en API, la vista previa), el máster, el pie y visual-qa-gate.md, y exige «VERDICT: PASS»; el revisor solo comprueba que no haya texto ni imagen cortados o tapados. Si FAIL, corrige una vez y repite. Si vuelve a FAIL, abandona la celda (en teléfono, regla del borrador abierto) y regístralo: pon la celda en `"status": "blocked"` con `reason_if_blocked_or_unsupported` en coverage.json para que la siguiente ventana no la vuelva a elegir.
   d. `lab.py preflight` y anota `espera` (producción cercana) y `versiones` en el run. No frena la publicación.
   e. Publica. Teléfono: ejecuta los comandos de `publicar` de la receta con las mismas sustituciones (valores con espacios entre comillas dobles; nunca dejes `<…>` ni corchetes en la línea); lo que va entre corchetes (p. ej. `[--produccion-cercana]`) se añade, sin los corchetes, solo cuando se cumple lo que dice su `ver` (`--produccion-cercana` si el último `preflight` trajo `espera` distinto de null: una publicación de producción cercana puede cuadrar el contador por casualidad). Sale con 0 solo con un estado de `estados_ok`. Con 4 antes de pulsar (por ejemplo, siguen visibles sugerencias del teclado o un desplegable) es un bloqueo esperado: no reintentes en esta ventana. Con 5 (un estado fuera de `estados_ok`, como `confirmado_sin_conteo`, `conteo_no_cuadra`, `sin_banner`, `timeout`, `fallido` o `error_tras_pulsar`) el envío es dudoso: no repitas nada y concilia como dice `conciliacion` de la receta (solo `telefono-captura`, nunca la regla del borrador abierto) antes de registrar. API: `gh workflow run media-lab -f manifest=<ruta>`; consigue el id y síguelo (ver Reglas: `--workflow media-lab.yml`, `displayTitle` con `<ruta>`); descarga con `gh run download <id> -n media-lab-result-<id> -D experiments/media-lab/results/<id>` y lee `experiments/media-lab/results/<id>/live-result.json`. Con los post_id de ese resultado: `lab.py manifiesto-verificacion --run-group <el mismo> --post facebook=<id> …` (solo feed: si la celda es solo de historias, sáltate este paso de verificación), commit y push de ese manifiesto, y `gh workflow run media-lab-verify -f manifest=<ruta>` igual que arriba (`--workflow media-lab-verify.yml`, artefacto `media-lab-verification-<id>` con `verification-result.json`).
   f. Verifica: ejecuta los comandos de `verificar` de la receta y sigue su `verificacion`. Si con los comandos permitidos no puedes obtener la URL de una publicación por teléfono, no la inventes: déjala en `missing_data_reasons.post_url` del run y sigue. Cada entrada de `copias` (por ejemplo, la copia automática de Instagram en Facebook) va en `publication.cross_posting` del run como publicación extra, no como celda.
   g. Actualiza el run (en teléfono, también `publication.native_app_and_version` con la versión de la app del último preflight), la celda de coverage.json, `lab.py encargo-usado --encargo ID --run RUN` y experiments/media-lab/progress.md.
8. Métricas: para cada instantánea vencida (24 h, 72 h, 7 d; cada Story en la primera ventana en que tenga entre 4 h y 20 h desde que se publicó o, si no hubo ninguna en esa franja, en la última ventana antes de que caduque a las 24 h) de un run publicado, en orden. El run_group base de ese run es su `run_id` completo, con la red: `LAB-F12-001-A-FACEBOOK` → `LAB-F12-001-A-FACEBOOK`, `LAB-F12-001-A-INSTAGRAM-STORY` → `LAB-F12-001-A-INSTAGRAM-STORY` (varias redes comparten el mismo grupo y un `-M24H` común colisionaría; mismo criterio para runs publicados por teléfono y para runs de antes de este cambio). `manifiesto-verificacion` no pisa un manifiesto que ya exista, y el del paso 7e (o el de una instantánea anterior) ya ocupa `<run_group base>-verify.json`: usa un run_group propio por instantánea, `<run_group base>-M24H`, `-M72H`, `-M7D` o `-MSTORY`, y pasa exactamente ese mismo valor a `metricas-registrar --run-group`:
   a. `lab.py manifiesto-verificacion --run-group <run_group base>-M24H --post <red del run>=<id>` (una sola red: la de este run, nunca las demás celdas del mismo grupo) — usa `--instagram-shortcode <shortcode del permalink>` en vez de `--post instagram=<id>` si esa red es Instagram publicado por teléfono, y añade `--superficie instagram=story` si es una Story de Instagram por API. Las Stories de Instagram publicadas por teléfono no se buscan por shortcode (el comando lo rechaza) y, como las de Facebook, no tienen métricas por API: no intentes verificarlas, anota directamente `missing_data_reasons.audience_metrics` y sigue.
   b. Commit y push de ese manifiesto.
   c. `gh workflow run media-lab-verify -f manifest=<ruta> -f metricas=true`; consigue el id y síguelo (ver Reglas: `--workflow media-lab-verify.yml`, `displayTitle` con `<ruta>`); descarga con `gh run download <id> -n media-lab-verification-<id> -D experiments/media-lab/results/<id>`.
   d. `lab.py metricas-registrar --run RUN --run-group <run_group base>-M24H --resultado experiments/media-lab/results/<id>/verification-result.json --instantanea 24h|72h|7d|story`.
   e. Commit y push del run y de coverage.json.
   Si algo de esto falla (el workflow, la descarga, un `lab.py` con código 2 — por ejemplo porque el resultado no está `verified` o su post_id no coincide con el del run), no bloquees la ventana: anota en `missing_data_reasons.audience_metrics` del run qué instantánea venció y por qué no se pudo, y sigue con la siguiente.
9. Commit solo de rutas propias: `git add experiments/media-lab/<rutas concretas>`, `git commit -m "media lab claude: …"`, `git fetch origin main`, `git rebase origin/main` y `git push origin HEAD:main`. Si hay conflicto: `git rebase --abort` e informa.
10. `lab.py lock-soltar --dueno programada`.

## Informe
Un borrador que no pudiste cerrar va al principio. Si no publicaste nada y no hubo fallo, basta con una línea con el motivo (por ejemplo, ningún encargo aprobado). Si publicaste, da celda, red, ruta, URL y veredicto de QA. Si algo bloqueó, di qué y qué decisión hace falta del usuario.
